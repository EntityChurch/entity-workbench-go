# Usage — revision history

Fixture doc whose path carries the word "revision" (so `find` can hit it) and
whose body mentions manual mode (so `grep` can match it).

In manual mode the revision log grows per `revision commit`, not per write.
